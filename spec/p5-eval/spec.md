# P5 Spec：评测（⏸️ 后置，暂不实现）

> 状态：**后置** ｜ 明确：在 P1-P4 跑通之前**不实现评测功能、不建评测平台**
> 完整方法论见根目录 `研究计划.md` §4（三维评分卡 + 跨层归因 + 元评测）。

## 1. 目标（占位）
内容 × 行为 × 集成 三维评分矩阵 + 跨层归因报告 + 元评测（人工锚点 → VLM judge 对齐）。

## 2. 依赖
- P1-P4 至少各有一个稳定 demo 可被评测
- 评测方法论先行：先人工打分表，再自动指标，再 judge 对齐
- 可复用：multica agent-eval 平台的多维评分卡设计经验（运行时/成本/资源/过程质量）

## 3. 触发时机
- 本 spec 只在 ROADMAP Part 5 启动时展开为完整 spec + tasks

## 4. 明确不做（现在）
- 不实现评测代码 / 评测平台 / 评分卡 UI
- 不把评测作为当前任何 task 的交付物（P1-P4 的验收用"人工确认+清单"即可）
