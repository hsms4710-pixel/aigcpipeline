# 工作流设计：角色 AIGC → AI NPC → 引擎接入 → 评测（多引擎视角）

> 日期：2026-08-12 ｜ 说明：本文档基于广泛调研（多引擎 / 开源实现 / 论文 / 商业管线），
> 聚焦「管线怎么衔接、做成什么形式、用户如何看到每阶段产出」，并给出 MVP 收敛建议。

---

## 0. 核心洞察（调研结论）

1. **引擎不该绑定单一平台**：已有引擎无关方案可直接借鉴（见 §2），角色资产应输出**通用格式**（立绘 PNG / 语音 WAV+字幕 / 3D glTF），再按引擎导入，而非为 Godot 定制死。
2. **"把角色接进现有游戏"是真实且可行的路径**：Convai 有 Modding 框架（UE 4.12–5.2 注入 AI NPC）、StardewLivingNPCs（星露谷物语 Mod，AI NPC + 白名单安全动作）、BepInEx 注入——不是必须从 0 建游戏。
3. **全链路统一评测是空白点**：图像/语音/行为各有评测，但"生成→接入→运行"端到端可归因评测没有开放框架。
4. **每阶段产物都应可见**：参考 3dModelGenerator（job 状态轮询）、studiomi300（按场景 streaming 输出）——管线要"可观察、可暂停、可回放"。

---

## 1. 五部分与衔接（含跨引擎）

```
P1 角色形象+语音生成
  ├─ 2D：立绘+表情差分（PNG/图集）→ 可直接被 Godot/Unity/网页使用
  ├─ 3D：TripoSR 图生3D → glTF/GLB（引擎无关）
  └─ 语音：GPT-SoVITS（5s零样本克隆+TTS）→ WAV + 字幕文件
        │
        ▼
P2 过场动画 AIGC ──（用 P1 资产；可先做"程序化过场"：Dialogic/时间轴+表情+运镜）
        │
        ▼
P3 AI Agent（Memory-RAG，复用 NPCSimulateGameAgent）
  协议层：HTTP / WebSocket / MCP（引擎侧只需标准 HTTP/WS 客户端）
        │
        ▼
P4 角色实体化 ── 两种模式：
   A. 从0建游戏（Godot 场景：状态机+寻路+交互+任务）
   B. **接入现有游戏**（Mod/注入：白名单安全动作，参考 StardewLivingNPCs / Convai Modding）
        │
        ▼
P5 评测（形象一致性 / 语音MOS / 行为质量 → 统一评分卡 → 反馈再生成）
```

**衔接设计原则**：
- **资产中立**：P1 只产出标准资产（图/声/glTF/字幕），任何引擎都能导入 → 引擎层只做"导入 + 运行时"
- **Agent 与引擎解耦**：Agent 是独立服务，引擎通过 HTTP/WS/MCP 调用 → 换引擎不换 Agent
- **事件总线**：游戏事件 → Agent 感知；Agent 决策 → 白名单动作执行（安全可审计）

---

## 2. 多引擎调研（不是只有 Godot）

| 方案 | 类型 | 引擎 | 亮点 |
|---|---|---|---|
| **OpenGameAgent** | 开源 | Godot 4.7 .NET + Unity 6 | agent kernel（流式消息/工具循环/steering）+ 游戏感知原语，可直接借鉴 |
| **Narra** | 开源 SDK | 引擎无关（HTTP） | TypeScript server，Godot/Unity/UE 都只是 HTTP 客户端 |
| **HoloScript Foundation** | 开源 | 40+ 目标 | 一份语义源编译到 Unity/Unreal/Godot/glTF/USD/WebGPU…（资产中立极佳参照） |
| **Gemma4NPC-it** | 开源模型 | Unity/UE/Godot | NPC 对话 + 游戏状态 JSON 输出（quest flag/交易/情绪） |
| **MindFox** | 开源中间件 | 引擎无关 | 离线 NPC 记忆中间件（SQLite，无云） |
| **Inworld Character Engine** | 商业 | UE/Unity | Ubisoft NEO NPCs；性格/知识/场景上下文 |
| **Convai** | 商业 SDK | Unity/UE | 四层架构（Runtime/Room/Agent/Module）；**Modding 框架可注入已发布 UE 游戏** |
| **NVIDIA ACE** | 商业 | UE | Audio2Face 实时面部动画 |
| **StardewLivingNPCs** | 开源 Mod | 星露谷 | AI NPC 注入现有游戏 + 白名单动作校验（最佳"接入现有游戏"参照） |

**结论**：引擎选 **Godot 为主**（简单/开源/节点体系贴合），但**资产与 Agent 层保持引擎无关**，预留 Unity 导入（OpenGameAgent 已证明双引擎可行）。

---

## 3. 相关开源 / 论文 / 生产管线速查

**AIGC 角色/资产管线**
- GoogleCloudForGameDesigner：Gemini 对话式生成概念图→编辑→配音（本地 Web 应用形态）
- Game Maker：DALL-E 3 + Flux 生成角色/场景/道具，2D 图转 3D
- Shinsekai（AI RPG maker，中文）：立绘/演出/TTS 一体
- aituber-kit / handcrafted-persona-engine：Live2D + LLM + ASR + TTS + RVC 的虚拟人栈（**与本课题 P1 最贴近的现成参考**）
- TripoSR / abstract3d：单图→3D mesh + UV 烘焙贴图（glTF 输出）

**过场 AIGC（P2）**
- **Cutscene Agent（论文）**：MCP 工具包 + director 多智能体编排 3D 过场（双向引擎集成）——本课题 P2 的最强参照
- studiomi300：脚本→6镜头分镜→关键帧→配乐→配音→30s mp4 全自动管线（有 streaming 输出形态）

**AI NPC 评测（P5）**
- **Orak（Krafton）**：12 个视频游戏的 LLM agent 评测基准
- **M3-BENCH**：混合动机游戏中的过程感知社交行为评测（过程维度 = 你的 agent-eval 思路一致）
- FAIRGAMER：LLM NPC 社会偏见基准
- FlashAdventure：GUI agent 冒险游戏基准

**语音（P1）**
- GPT-SoVITS：5s 零样本克隆 + TTS + 语速控制（已验证，WebUI 生态成熟）

---

## 4. 管线做成什么形式？（衔接体验）

目标形态：**「人设卡 → 可交互 NPC」在线工作台**，让用户看到每个阶段的产物与进度。

```
[人设卡/参考图上传]
   → Job 队列（参考 3dModelGenerator 的 job 轮询）
   → 阶段状态机：concept → portrait → voice → model(3D) → engine_package → agent_ready
   → 每阶段产物可预览/下载（立绘缩略图、语音播放器、glTF 预览器、NPC 沙盒试玩）
   → 失败可重试单阶段（不整条重跑）——"阶段级可观察、可暂停、可回放"
```

**为什么是"工作台"而非"一键生成"**：
- AIGC 生成有随机性，用户要**在每阶段确认/挑选**（选图、选声线）
- 全链路自动跑通但结果差时，**要能定位是哪一段**（呼应评测归因）
- 分阶段预览本身就是内容素材（账号截图/录屏）

**接入形式（对使用者）**：
- 导出物 = 标准资产包（图/声/glTF/人设 JSON/agent 配置），用户拖进任意引擎
- 或一键生成 Godot 插件包 / Unity package / UE Mod（三选一，先做 Godot）

---

## 5. MVP 建议（聚焦一个 part）

> 不要一上来做全链路。**MVP = P1「角色形象+语音生成管线」，但以"工作台"形态交付**。

理由：
1. P1 是 P2/P4 的输入，先做它所有后续才有料
2. 它独立、可 demo、可分享（账号内容友好）
3. 工作台的"阶段产物可视化"雏形在 P1 就能建立（概念图→立绘→语音），后续 part 只是加阶段
4. 现成参考多（handcrafted-persona-engine / GPT-SoVITS / Flux+LoRA），2-3 周可出可玩版本

**P1 MVP 范围（收窄）**：
- 输入：一段人设描述 + 可选参考图
- 输出：角色立绘（2-4 表情差分）+ 3 句角色台词的克隆语音 + 资产包下载
- 形态：本地 Web 工作台（上传→看概念图→选图→听语音→下载）
- 评测（P5 前置最小版）：立绘一致性人工评分 + 语音 MOS，先人工锚点
- 并行：P3 agent 接入作为第二条轨道同步推进（零依赖）

---

## 6. 落地里程碑（工作流视角）

| 里程碑 | 内容 | 产出 |
|---|---|---|
| M0 | Godot + 生图 + GPT-SoVITS 工具链跑通 | 一张图+一句话 demo |
| M1 | P1 工作台 v0（人设→立绘→语音→下载） | 本地 Web 应用 + 资产包 |
| M2 | P3 Agent 服务 + Godot 接入（HTTP/MCP） | NPC 能对话 |
| M3 | P4 实体化（Godot 场景：交互/寻路/任务） | 可玩单 NPC demo |
| M4 | P5 评测接入（形象/语音/行为 → 评分卡） | 评测报告自动生成 |
| M5 | 开源发布 + 教程 + 场景演示 | GitHub + 账号内容 |