# 研究计划：AIGC 角色 × AI NPC × 引擎接入 × 全链路评测

> 日期：2026-08-12 ｜ 配套文档：《工作流设计.md》（管线衔接 / 多引擎 / 开源参照 / 工作台形态）
> 本文档回答：研究什么问题、分几层、评测怎么做、按什么节奏做、看什么文献。

---

## 1. 目标与定位

**一句话目标**：把「角色 AIGC 生成（形象/语音/3D）→ AI NPC（记忆/行为/对话）→ 引擎接入（新建游戏或接入现有游戏）→ 多维度评测」打通成一条**可观察、可归因、可复用的在线工作流**，并沉淀为可开源的研究课题。

**三层定位（避免自嗨）**：
- 研究层：全链路"跨层归因评测"目前没有开放框架 → 这是论文级空白点
- 工程层：每个 part 复用成熟开源组件，重在**衔接与编排**而非从零造轮子
- 产品层：以"工作台"形态交付，让使用者看到每阶段的产物、进度、可重试点

**不做**（第一版明确排除）：
- 不做从 0 自研生图/语音/3D 模型（全部用开源模型 + 微调）
- 不做商业化大平台，先做本地/单机可跑的工作台
- 不承诺"一键出完整可玩游戏"，MVP 聚焦单个 part（见 §6）

---

## 2. 研究问题（RQ）

| 编号 | 研究问题 | 为什么重要 | 相关文献/参照 |
|---|---|---|---|
| **RQ1** | 角色资产如何在**引擎无关**的前提下生成并导入多引擎（Godot/Unity/UE）？"通用资产 + 引擎适配器"模式成立吗？ | 避免绑定单一引擎；接入现有游戏才可能 | OpenGameAgent（双引擎）、HoloScript（40+ 目标）、glTF/USD 资产中立 |
| **RQ2** | 如何把**一个 AI NPC 接入已有游戏**（而非从 0 建游戏）？Mod/注入的安全动作白名单机制如何设计？ | 用户真实需求是"把人物接入游戏"，星露谷/Convai 已证明可行 | StardewLivingNPCs、Convai Modding、BepInEx |
| **RQ3** | AI NPC 的**记忆/角色一致性/行为质量**如何端到端评测？"过程维度"如何纳入评分而不是只看结果？ | 这是 agent-eval 平台经验的延伸；M3-BENCH 等已把过程纳入 | M3-BENCH、Orak、CharacterBench、RoleplayEval、HEART-BENCH、persona fidelity 原子级评测 |
| **RQ4** | 全链路"**跨层归因**"：当最终 NPC 表现差时，问题在形象/语音/Agent/引擎哪一层？能否自动定位并反馈到该层重新生成？ | 全链路统一评测的空白点，也是产品上"阶段可重试"的依据 | 3dModelGenerator job 状态机；agent-eval 平台的"运行时/成本/资源"多维评分经验 |
| **RQ5** | 资源与性能：本地 LLM/TTS 接入对游戏 **CPU/GPU/RAM/VRAM** 的影响如何量化？云/本地如何取舍？ | 已有论文测过 LLM NPC 的资源占用，但没做成评测维度 | Enriching Gameworlds With LLM NPCs（资源基准）、NVIDIA ACE 云方案 |

---

## 3. 五层架构（研究视角）

```
L0 资产层    人设卡(JSON) · 参考图 · 参考语音 · 世界观/文风
   │
L1 生成层    P1 形象(2D立绘/表情差分/3D glTF) · P1 语音(克隆TTS) · P2 过场
   │         每个生成任务 = 可追踪 Job（输入快照/参数/产物/耗时/成本/失败重试）
   │
L2 认知层    P3 AI NPC Agent：记忆(Memory-RAG) · 角色人设 · 工具/白名单动作 · 多轮对话
   │         引擎无关协议：HTTP / WebSocket / MCP
   │
L3 集成层    P4 引擎运行时：新建游戏(Godot 场景) 或 接入现有游戏(Mod/注入)
   │         事件总线：游戏事件 → Agent 感知；Agent 决策 → 白名单动作执行
   │
L4 评测层    P5 统一评分卡：内容 × 行为 × 集成 三维矩阵 + 跨层归因 → 反馈 L1/L2 重生成
```

**每层的产出都必须"可见"**：L1 出预览图/语音播放/3D 预览；L2 出对话沙盒；L3 出可玩 demo/录屏；L4 出评测报告。所有中间产物落盘到工作台的项目目录（参考 multica 各 group 独立工作文件夹的做法）。

---

## 4. 统一评测设计（核心创新点）

### 4.1 评测矩阵：内容 × 行为 × 集成

借鉴 agent-eval 平台经验（运行时/成本/资源/通信等多维评分，而非单一通过率）+ 最新基准，设计三维矩阵：

| 维度 | 子指标 | 评测对象 | 方法/工具 |
|---|---|---|---|
| **内容质量** | 形象一致性（身份保持） | L1 立绘/3D | ViStoryBench 式：CLIP 相似度 + 人工锚点；CCED(DeepFace+CLIP+LPIPS) |
| | 风格一致性 | L1 | style similarity（ViStoryBench） |
| | 语音自然度/相似度 | L1 语音 | MOS-N / S-MOS / UTMOS；SIM / WER-CER |
| | 表情差分可用性 | L1 | 图集完整性 + 情绪标签命中率 |
| **行为质量** | 角色一致性（persona fidelity） | L2 对话 | CharacterBench 11 维 / RoleplayEval 7 维（事实一致/认知一致/风格对齐/多轮稳定）；原子级 OOC 检测（ACL25） |
| | 记忆一致性（跨会话） | L2 记忆 | HEART-BENCH（基于原始情景记忆评估） |
| | 任务完成度/过程质量 | L2 | M3-BENCH 过程感知；Orak 12 游戏基准 |
| | 社会偏见/安全性 | L2 | FAIRGAMER |
| **集成质量** | 引擎可导入性 | L3 | 资产校验（glTF 合法性/纹理缺失/命名规范） |
| | 运行时资源占用 | L3 | CPU/GPU/RAM/VRAM 增量基准（参考 LLM NPC 资源论文） |
| | 延迟/并发 | L3 | 对话响应延迟 P50/P95；多 NPC 并发 |
| | 接入破坏度（对现有游戏） | L3 | Mod 注入回归测试（不破坏原游戏任务/存档） |
| **成本/工程** | 单角色全链路成本（API/GPU 时长） | L0-L4 | 成本归一化复合分（agent-eval 平台已有） |
| | 可复现性 | L0-L4 | 同一输入多试次重复运行的稳定性（pass^k） |

### 4.2 跨层归因（RQ4 的实现路径）

```
最终分低 → 逐层定位：
  L1 内容分低？ → 换生图/换声线/加 LoRA 锚点，单层重试（不重跑全链路）
  L2 行为分低？ → 换 prompt/加记忆/换模型
  L3 集成分低？ → 引擎适配/资源优化
  → 输出"归因报告"：哪层贡献了多少分、拖累了多少分（分层得分 + 消融）
```

**为什么这是空白点**：图像/语音/行为各有评测，但"生成→接入→运行"的端到端归因没有开放框架。CutsceneBench 只覆盖过场，Orak 只覆盖行为——把两者接到同一条管线上并做跨层归因，是论文贡献。

### 4.3 评测自身也在进步（元评测）

- 阶段一：人工锚点（评分员 3 人 + 打分标准文档）
- 阶段二：VLM/LLM-as-judge 自动打分（ViStoryBench/UTMOS 思路），与人工锚点对齐率作为 judge 质量指标
- 阶段三：基准集积累 → 开放 benchmark（参考 CutsceneBench 分层设计）

---

## 5. 技术选型矩阵（每个 part 的候选与取舍）

| Part | 候选方案 | 首选（MVP） | 备选 | 关键取舍 |
|---|---|---|---|---|
| P1 形象-2D | Flux+LoRA / SDXL+LoRA / DALL-E3 | Flux+LoRA（可控性+一致性） | SDXL（生态成熟、显存低） | 一致性靠 LoRA 锚点 + 表情差分模板 |
| P1 形象-3D | TripoSR / abstract3d | TripoSR → glTF | abstract3d（UV 烘焙） | 2D 够用时先不做 3D |
| P1 语音 | GPT-SoVITS / CosyVoice / F5-TTS | GPT-SoVITS（5s 零样本克隆，已验证） | CosyVoice3 | 参考音频质量决定克隆上限 |
| P2 过场 | Cutscene Agent（MCP+director）/ Dialogic | 先做"程序化过场"（Dialogic+表情+运镜） | studiomi300 式全自动 | P2 依赖 P1 资产，放 M2+ |
| P3 Agent | 自研 Memory-RAG + 现有 NPC agent | 复用 NPCSimulateGameAgent + Memory-RAG | Gemma4NPC-it（本地） | 引擎无关协议 HTTP/WS/MCP |
| P4 引擎 | Godot（主）/ Unity / Mod 注入 | Godot 场景（状态机+寻路+交互） | Convai Modding（UE 已发布游戏） | 先新建游戏，接入现有游戏做 POC |
| P5 评测 | 自研评测台 | 阶段一人工锚点 + 自动指标 | 开放基准集 | 复用 agent-eval 平台的多维评分卡 |

---

## 6. 分阶段 Roadmap

### 阶段 0：地基（1-2 周）——M0
- Godot 工程 + 生图工具链 + GPT-SoVITS 跑通，产出"一张图 + 一句话"demo
- 定义人设卡 JSON Schema（L0 契约）
- 评测矩阵初版文档 + 人工打分表

### 阶段 1：P1 工作台 MVP（3-4 周）——M1（**聚焦单个 part，先行交付**）
- 输入：人设描述 + 可选参考图/参考语音
- 输出：角色立绘（2-4 表情差分）+ 3 句台词克隆语音 + 资产包（图/WAV/字幕/人设 JSON）
- 形态：本地 Web 工作台（Job 队列 + 阶段状态机 + 每阶段预览/下载/单步重试）
- 评测：形象一致性人工评分 + 语音 MOS，先人工锚点
- **并行轨道**：P3 Agent 服务原型（零依赖，单独推进）

### 阶段 2：Agent + 引擎接入（3-4 周）——M2/M3
- P3：Memory-RAG Agent 服务 + Godot 通过 HTTP/WS/MCP 接入，NPC 能对话
- P4：Godot 场景（交互/寻路/任务），可玩单 NPC demo
- 评测：行为维度接入（persona fidelity + 过程质量）

### 阶段 3：过场 + 实体化扩展（2-3 周）——M3+
- P2 程序化过场（依赖 P1 资产）
- 接入现有游戏 POC：选一个 Mod 友好的开源游戏（如星露谷）做白名单动作注入

### 阶段 4：全链路评测与归因（3-4 周）——M4
- 评测台：内容×行为×集成矩阵自动跑分 + 跨层归因报告
- 元评测：VLM-as-judge 与人工锚点对齐

### 阶段 5：开源与内容（持续）——M5
- GitHub 开源 + 教程 + 场景演示 + 账号内容（每阶段产物天然是素材）

**MVP 理由**（为什么先 P1）：P1 是 P2/P4 的输入、独立可 demo、可建立"阶段产物可视化"雏形、现成参照最多（handcrafted-persona-engine / GPT-SoVITS / Flux+LoRA），2-3 周可出可玩版本。P3 零依赖可并行。

---

## 7. 风险与开放问题

1. **角色一致性上限**：零样本生图一致性仍不稳 → 用 LoRA 锚点 + 表情模板兜底；评测用"跨图身份保持"指标量化
2. **语音克隆版权/伦理**：只克隆用户自己的参考音；资产包需声明用途
3. **接入现有游戏的技术债**：Mod 注入脆 → 先做白名单动作 + 回归测试，POC 选开源游戏
4. **本地资源瓶颈**：本地 LLM/TTS 显存占用 → 做资源基准，支持云/本地可切换
5. **评测主观性**：人工锚点先上，VLM judge 需要对齐率门槛（如 0.8）才替代

---

## 8. 文献与开源调研清单

### 8.1 引擎与资产中立
- OpenGameAgent（Godot 4.7 .NET + Unity 6，agent kernel + 游戏感知原语）
- Narra（引擎无关 TypeScript server，HTTP 接入）
- HoloScript Foundation（40+ 编译目标，资产中立参照）
- glTF / USD（资产中立格式）

### 8.2 AI NPC 与记忆
- Gemma4NPC-it（NPC 对话 + 游戏状态 JSON）
- MindFox（离线 NPC 记忆中间件，SQLite）
- Inworld Character Engine / Convai（商业；Convai Modding 可注入已发布 UE 游戏）
- NVIDIA ACE（Audio2Face 实时面部动画）
- StardewLivingNPCs（星露谷 Mod：AI NPC + 白名单安全动作）

### 8.3 AIGC 资产管线
- handcrafted-persona-engine / aituber-kit（Live2D+LLM+ASR+TTS+RVC 虚拟人栈，P1 最贴近）
- TripoSR / abstract3d（单图→3D glTF）
- GPT-SoVITS（5s 零样本克隆 TTS）
- GoogleCloudForGameDesigner / Game Maker / Shinsekai（AI RPG maker，立绘/演出/TTS 一体）

### 8.4 过场生成
- Cutscene Agent（论文 2604.25318：MCP + director 多智能体 3D 过场 + **CutsceneBench 分层评测基准**）
- studiomi300（脚本→分镜→关键帧→配乐→配音→mp4 全自动）

### 8.5 评测基准与论文
- Orak（Krafton，12 游戏 LLM agent 基准）
- M3-BENCH（混合动机游戏过程感知社交行为评测）
- FAIRGAMER（LLM NPC 社会偏见）
- FlashAdventure（GUI agent 冒险游戏基准）
- CharacterBench（AAAI 2025：LLM 角色定制，22,859 样本 / 3,956 角色 / 11 维 6 方面）
- RoleplayEval（7 维：事实/认知一致性、风格对齐、多轮稳定）
- ViStoryBench（故事可视化：角色一致性/风格相似/提示对齐/美学/伪影）
- CCED + Theseus（DeepFace+CLIP+LPIPS 角色一致性评测集）
- Atomic-Level Persona Fidelity（ACL 2025 Findings：越界行为原子级检测）
- HEART-BENCH（基于原始情景记忆的角色一致行为评测）
- Beyond Simple Personas（SIGDIAL 2025：角色一致对话评测）
- TTS 评测：MOS-N / S-MOS / UTMOS / SIM / WER-CER（零样本克隆评测框架）
- LLM NPC 资源占用基准（Enriching Gameworlds With LLM NPCs：CPU/GPU/RAM/VRAM 增量）

### 8.6 本课题可复用资产
- multica 内 agent-eval 平台：多维评分卡（运行时/成本/资源/通信/过程质量）设计经验
- multica 内 NPCSimulateGameAgent / NPC 评测：行为维度基础
- 简历上的 Agent 评测 / RAG / NPC 项目：chunk 设计（ngram 离线确定性 embedding + 可选 text2vec）、RAGAS 五指标、多试次稳定性——可直接迁移为 L2/L4 的实现基础

---

## 9. 下一步（本周可启动）

1. **P1 工作台技术验证**：Flux+LoRA 跑 1 个角色 4 表情差分；GPT-SoVITS 克隆 3 句话；记录耗时/显存/成本（喂给评测矩阵）
2. **人设卡 JSON Schema v0**：定义 L0 契约（人设/文风/参考资产/配音需求）
3. **评测矩阵落表**：把 §4.1 转成评测台的数据库 schema（指标/权重/数据来源/人工or自动）
4. **P3 Agent 服务骨架**：Memory-RAG + HTTP 协议，先跑通"无游戏也能对话"的沙盒
