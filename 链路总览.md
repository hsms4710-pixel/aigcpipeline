# 链路总览：全链路 / Part 范围边界 / 分 Part 验证

> 本文档是"整个链路怎么走、每个 Part 做什么不做什么、怎么验证"的权威视图。
> 与 README（总览）、ROADMAP（节奏）、各 spec（细节）配套；冲突以本文档 + 对应 spec 为准。

## 0. 全链路一图流
```
人设卡/参考资产（文字 / 参考图 / 参考音）
   │
   ▼
P1 角色生成 ───────────────► P2 过场（⏸️ 后置，用 P1 资产）
  形象（立绘分层）+ 语音           （AIGC 视频 / 引擎内）
   │                              │
   └──────► P4 引擎接入 ◄──────── P3 AI NPC Agent
                 │                （记忆/对话/白名单动作，引擎无关）
                 ▼
        可玩 NPC demo（Godot 新建 或 Mod 接入现有游戏）
                 │
                 ▼
        P5 评测（⏸️ 后置：内容×行为×集成 + 跨层归因）
横切：P6 DCC 加工（Blender-MCP：清理/UV/绑定/表情morph/导出）—— 资产进引擎前必经
```

## 1. Part 范围边界总表

### P1 角色形象 + 语音生成（⏳ 先行，MVP）
| 项 | 内容 |
|---|---|
| 目标 | 工作台：人设卡+参考图/音 → 立绘（分层）+ 克隆语音 → 标准资产包 |
| 输入 | `persona.json` + 参考图 + 参考音 |
| 输出 | 资产包：portrait/（full + layered 分层 + expressions 可选）、voice/（WAV+字幕）、metadata.json |
| ✅ 做 | 2D 立绘生成（云 API：GPT Image 2 / Nano Banana Pro；本地 ComfyUI 可选）、分层输出、语音克隆（TTS 多后端：CosyVoice 本地 + 火山/Azure 云）、工作台 Web（Job 队列+阶段状态机+预览/重试/下载）、资产校验脚本 |
| ❌ 不做 | 3D 生成（后置 P1 扩展）、引擎运行时、Live2D 绑定（后置 P4）、过场、评测 |
| 验证 | t1 工具链（生图一致性/质量/成本 + TTS 多后端）→ t3/t4 单模块 CLI → t5 工作台端到端 → t6 Godot 最小工程导入 |
| gate | P1 验收全过 + Godot 导入成功 + 审计记录 |
| 依赖 | 隔离环境（Docker/WSL2/云机）+ 云 key（GPT/Gemini/TTS） |

### P3 AI NPC Agent（规划中，Part 2）
| 项 | 内容 |
|---|---|
| 目标 | 引擎无关 Agent 服务：Memory-RAG + 多轮对话 + 白名单动作；先沙盒 |
| 输入 | 人设卡 + 世界观（角色注入） |
| 输出 | Agent 服务（HTTP/WS/MCP）+ 对话沙盒 Web + 记忆存储（SQLite/向量） |
| ✅ 做 | 角色注入（人设→system prompt）、多轮对话、跨会话记忆、白名单动作（JSON 输出+校验+拒绝记录）、协议（至少 HTTP） |
| ❌ 不做 | 游戏逻辑、UI、引擎集成（P4）、语音/情感（可选扩展）、P5 评测 |
| 验证 | 沙盒多轮对话不串角色；跨会话记忆召回；HTTP 协议通；非白名单动作被拒 |
| gate | 四项验收全过 |
| 依赖 | LLM（本地 Ollama 或 API）、人设卡 schema（P1 t2） |

### P4 引擎接入（规划中，Part 3）
| 项 | 内容 |
|---|---|
| 目标 | NPC 实体化：Godot 场景可交互（显示/语音/对话/表情）+ 引擎内动画；Mod POC 接入现有游戏 |
| 输入 | P1 资产包 + P3 Agent + P6 加工后资产 |
| 输出 | Godot 单 NPC demo + 白名单动作执行器 + 事件总线 +（POC）Mod 注入 |
| ✅ 做 | 资产导入、对话 UI、事件总线（游戏事件→Agent；Agent 决策→白名单）、引擎内动画（2D Live2D/Spine 表情口型；3D blendshape+骨骼）、Mod 注入 POC（SMAPI/BepInEx） |
| ❌ 不做 | 完整游戏、多 NPC、网络/联机、复杂寻路（先简单） |
| 验证 | demo 可玩（立绘+语音+多轮对话+表情）；白名单执行正确、越权被拒；Mod POC 回归检查不破坏原游戏 |
| gate | 验收 + 回归检查通过 |
| 依赖 | P1、P3、（P6 S3） |

### P2 过场 AIGC（⏸️ 后置）
| 项 | 内容 |
|---|---|
| 目标 | 过场：A AIGC 视频播片 / B 引擎内程序化 / C 混合 |
| 输入 | P1 资产（角色/语音）+ 剧本（Ink） |
| 验证 | M2-1 拆镜 → M2-2 视觉资产（GPT/Nano Banana）→ M2-3 引擎内过场 demo → M2-4 AIGC 视频 POC |
| 依赖 | **主链（P1→P3→P4）打通后再启动** |
| 不做 | 在主链打通前不开发 |

### P5 评测（⏸️ 后置，不实现）
| 项 | 内容 |
|---|---|
| 目标 | 内容×行为×集成 三维评分卡 + 跨层归因 + 元评测 |
| 依赖 | P1-P4 各有一个稳定 demo |
| 不做 | 在 P1-P4 跑通前，不实现评测功能/平台 |

### P6 DCC 加工（横切，规划中）
| 项 | 内容 |
|---|---|
| 目标 | Agent 通过 DCC-MCP 控制 Blender 等完成资产加工（清理/UV/PBR/绑定/表情 morph/导出） |
| 场景 | S1 3D 加工（P1 3D 扩展）→ S2 2D 分层绑定 → S3 引擎导出（P4）→ S4 过场（P2）→ S5 Mod 辅助 |
| 依赖 | dcc-mcp-creator（intern-learn 664011）+ layout-forge dccBridge |
| 不做 | 不重写 DCC-MCP 框架；先 Blender headless + 3 skill |

## 2. 分 Part 验证策略（每 Part 交付前自验证，不依赖 P5）
| Part | 验证内容 | 方式 |
|---|---|---|
| P1 | 资产质量（身份一致/音色一致）+ 资产包结构 + 端到端工作台 | 人工锚点（肉眼/试听）+ 校验脚本 + 端到端跑通 |
| P3 | 对话稳定/记忆召回/协议/白名单 | 沙盒实测（多轮/跨会话/越权用例） |
| P4 | demo 可玩 + 动作白名单 + Mod 回归 | 实机 demo + 回归检查 |
| P2 | 过场可播放 | demo 播放 |
| P5 | 统一评分卡（后置） | 方法论先行：人工锚点 → VLM judge |
- 所有验证的**耗时/成本/样本**记入 `harness/memory/knowledge/`，为 P5 积累素材
- 每个 task 验收栏必须有验证输出（verify 门禁，见 harness/verify.md）

## 3. 边界红线（防 scope 蔓延）
1. P1 不做 3D / 引擎运行时 / Live2D 绑定（后置）
2. P3 不做游戏逻辑（白名单动作模拟即可）
3. P4 不做完整游戏（单 NPC demo + Mod POC）
4. P2 / P5 后置（主链打通前不开发）
5. 生图：云 API（GPT + Nano Banana）优先，本地 ComfyUI 仅可选离线后端
6. 资产中立 / 引擎无关（PNG/WAV/glTF/JSON）
7. 评测后置：P1-P4 的验收用"人工确认 + 清单"，不实现评测功能
8. 一个 Part 一个 Part 推进，做透一个再进下一个

## 4. 当前状态与下一步
- ✅ Part 0 地基；⏳ **Part 1（P1）**；⏸️ P2/P5 后置；📋 P3/P4/P6 规划
- 下一步：t1 工具链验证（GPT Image 2 / Nano Banana Pro 一致性 + TTS 多后端）→ t2 人设卡 schema → t3/t4 生成 CLI → t5 工作台 → t6 Godot 导入 → t7 审计归档
